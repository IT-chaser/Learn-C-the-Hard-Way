#include <stdio.h>
#include <stdint.h>

int main(int argc, char *argv[]) {
    // int_fast16_t myInt;
    // printf("int size is: %lu\n", sizeof(myInt));
    printf("int size is: %lu\n", SIZE_MAX);
    
    return 0;
}